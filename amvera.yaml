meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build   # опционально

run:
  command: npm run start              # или scriptName (см. ниже)
  containerPort: 3000                 # укажите порт, на котором слушает приложение
