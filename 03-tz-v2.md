# ТЗ v2 для ИИ‑агента · ПОТОЛКОВО, ветка `quizv2ver1` → `quizv2ver2`

**Версия:** 2.0 · **База:** коммит `e5fc955` · **Основание:** аудит №1 (соответствие `tz-agent.md`) и аудит №2 (глубокий аудит по 53 находкам `F‑xx`).
**Стек:** Next.js 16.1 (App Router, Turbopack) · React 19.2 · Tailwind 4 · TypeScript · vitest · Playwright · **PostgreSQL через Drizzle ORM (добавляется в N‑001)**.

Документ **дополняет**, а не заменяет `tz-agent.md`: правила раздела 0, прайс (2.3), схема событий (Приложение В), payload лида (Приложение Б) остаются в силе. Ниже — только новые/незакрытые задачи `N‑xxx` с файлами, действиями и приёмкой.

---

## 0. Правила (изменения относительно v1)

0.1. **Ветка** `quizv2ver2`. Один коммит = одна задача `N‑xxx`; сообщение `N-012: короткое описание (F-04, F-12)`.
0.2. **Порядок фаз:** A (P0, неделя 1) → B (P1, недели 2–3) → C (P2, недели 4–6). Фаза не начинается, пока предыдущая не зелёная.
0.3. **Разрешённые новые зависимости (снимает конфликт п. 0.3 v1 ↔ 2.4 v1):** `drizzle-orm`, `pg`, `@types/pg` (dev), `drizzle-kit` (dev). Больше ничего.
0.4. **Тексты `content/*.ts` менять можно** только в задачах, где это явно указано (N‑002, N‑030, N‑040, N‑060); каждое изменение — отдельным списком в описании коммита «было → стало».
0.5. **Зелёное состояние фазы:**
```
npm run lint              # 0 errors
npx tsc --noEmit          # 0 errors
npm run test              # vitest
npm run test:e2e          # ВСЕ 10 сценариев T‑091 + новые из N‑003
npm run build             # 18+ страниц, check:bundle ≤ 300 КБ
npm run check:legal       # только перед релизом (владелец заполняет реквизиты)
```
0.6. **Отчёт по каждой фазе** — `PHASE-A-REPORT.md`, `PHASE-B-REPORT.md`, `PHASE-C-REPORT.md` (в v1 отчёты по фазам 1–3 отсутствовали).
0.7. **Запрещено:** `setState` в `useEffect` для синхронизации состояний (остаётся 4 нарушения — устраняются в N‑050); новые файлы > 600 строк; литералы скидок/цен вне `content/pricing.ts`.
0.8. **Скриншот‑приёмка.** Для задач с меткой 📸 агент прикладывает к PR скриншоты Playwright (`page.screenshot`) desktop 1440×900 и mobile 390×844 до/после.

---

## 1. Целевые метрики v2

| Метрика | v1 факт | Цель v2 | Как измеряем |
|---|---|---|---|
| Лид сохранён в БД и доступен по `leadId` | 0 % (память) | 100 % | интеграционный тест route + `select` |
| Карточки каталога с локальным фото | 0 % | ≥ 95 % (остальные — честная заглушка с названием) | `tests/catalog-images.test.ts` |
| Видимая область контента Шага 1 (desktop 1440×900) | ≈ 150 px | ≥ 480 px | Playwright `boundingBox` контейнера |
| Видимая область контента Шага 0 (mobile 390×844) | ≈ 330 px | ≥ 480 px | то же |
| Обязательные e2e T‑091 | 3 из 10 | 10 из 10 + 6 новых | CI |
| Расхождение цены услуги hero ↔ секция цены ↔ `pricing.ts` | 2 страницы | 0 | `tests/service-price-anchor.test.ts` |
| Источников состояния калькулятора | 3 | 1 (`useCalculator`) | grep + code review |
| Строк в самом большом клиентском файле | 2 248 | ≤ 600 | `scripts/check-file-size.mjs` |
| Первый экран Шага 1 без действия (bounce) | неизвестно | −30 % | Метрика `lighting_step_view → lighting_kit_complete` |

---

## ФАЗА A — P0 (неделя 1). Перестать терять заявки и продажи

### N‑001 · Лиды в PostgreSQL через Drizzle · (F‑19, F‑48; закрывает T‑027)
**Файлы:** новые `db/index.ts`, `db/schema.ts`, `drizzle.config.ts`; `lib/lead/store.ts` (добавить `PgLeadStore`), `lib/lead/rate-limit.ts`, `app/api/lead/route.ts`, `app/api/lead/retry/route.ts`, `lib/env.ts`, `.env.example`, `README.md`, удалить `db/schema.sql`.
**Сделать:**
1. `db/schema.ts` — таблицы `leads`, `lead_deliveries` по Приложению Б v1 (поля 1:1 с `db/schema.sql`), индексы `leads_created_at_idx`, `leads_phone_created_idx`, `lead_deliveries_status_idx`. `db/index.ts` — `drizzle(new Pool({ connectionString: env.DATABASE_URL }))` с кэшем пула в `globalThis`.
2. `PgLeadStore implements LeadStore` — все 5 методов; `getLeadStore()` → `Pg` при заданном `DATABASE_URL`, иначе `InMemory` **с `console.warn` один раз на процесс**.
3. Дедуп: `findRecentByPhone` — `where phone = $1 and created_at > now() - interval '10 minutes'`. Rate‑limit: in‑memory Map остаётся, **плюс** серверная проверка `count(*) where ip_hash = $1 and created_at > now() - 10 min >= 5 → 429`.
4. `/api/lead/retry`: выбирает `lead_deliveries.status = 'failed' and attempts < 5`, доставляет, обновляет `attempts/last_error/sent_at`. Ответ — `{ retried, sent, failed }`.
5. `publicCode` генерируется с проверкой уникальности (retry при конфликте `unique`).
6. `README`: раздел «База данных» — провайдер (Neon / Vercel Postgres / Amvera Postgres), `npx drizzle-kit push`, cron для `/api/lead/retry` (Vercel Cron `*/15 * * * *` с `CRON_SECRET`).
**Приёмка:** `tests/lead-store-pg.test.ts` (vitest с `DATABASE_URL` тестовой БД, пропуск если не задан): создать → найти по телефону → записать failed доставку → `listFailedDeliveries` возвращает её; интеграционный тест route: 201 + строка в `leads` с `snapshot.rooms.length === 2`; 6‑й запрос с одного IP → 429 **после `resetLeadStoreForTests()`** (т.е. лимит держится на БД, а не на памяти).

### N‑002 · Ценовые якоря услуг из `pricing.ts`, H1 без региона · (F‑31, F‑32, F‑39; закрывает T‑046 п. 1, T‑020 остаток)
**Файлы:** `content/services.ts`, `content/pricing.ts` (`formatFrom`, `servicePriceAnchor`), `app/uslugi/_components/ServiceHero.tsx`, `ServicePriceSection.tsx`, `ServiceRelatedServices.tsx`, `content/service-links.ts`, `components/calculator-modal/price-strip.tsx`, `components/home/price-calculator-context.tsx`, `components/lighting/LightingCartDrawer.tsx`, `lib/seo-schema.ts`.
**Сделать:**
1. `content/pricing.ts`: `export function servicePriceAnchor(slug): { value: number; unit: "м²" | "м.п." | "линия" | null; label: string }` — таблица: `tenevoy-profil → shadowProfilePerM/м.п.`, `paryashchie-potolki → floatingProfilePerM/м.п.`, `svetovye-linii → lightLinesPerM/м.п.`, `trekovoe-osveshchenie → track.builtInPerM/м.п.`, `skrytye-karnizy → cornice.surface/м.п.` (**1 000**, с подписью «ниша от 1 800, встроенный от 4 500»), `prostye-potolki → ceiling.standard/м²`, `svetoprozrachnye-potolki → 4000/м²` (добавить `pricing.ceiling.translucentPerSqm = 4000`), `individualnye-proekty → null` («по проекту»), `prodazha-trekovogo-osveshcheniya → null` (вычисляется из комплектов, T‑014).
2. Удалить поля `hero.priceBadge` и `price.fromLabel` из `content/services.ts` (кроме `priceBadgeOverride` страницы света); компоненты читают `servicePriceAnchor(service.slug)`.
3. H1 всех 9 страниц — без «в Москве и МО» (регион остаётся в `seo.title` и `description`). Список «было → стало» в коммите. Пример: «Теневой профиль для натяжных потолков в Москве и МО» → «Теневой потолок без плинтуса: профиль и монтаж».
4. Убрать литералы `25/10` в `price-strip.tsx:91`, `price-calculator-context.tsx:366`, `LightingCartDrawer.tsx:152` → `pricing.lightingDiscount.*`.
5. JSON‑LD `Offer.price` — из `servicePriceAnchor`.
**Приёмка:** `tests/service-price-anchor.test.ts`: для каждого slug рендер‑строка hero === строка секции цены === `formatFrom(servicePriceAnchor)`; grep `priceBadge:|fromLabel:` в `content/services.ts` → 0 (кроме страницы света); grep `\b(25|10)\b` рядом с `discount|percent` вне `pricing.ts` → 0; grep «в Москве и МО» в `h1:` → 0.

### N‑003 · Полный обязательный e2e‑набор · (F‑49; закрывает T‑091)
**Файлы:** `e2e/funnel-standard.spec.ts`, `e2e/funnel-modern.spec.ts`, `e2e/service-preset.spec.ts`, `e2e/lighting-first.spec.ts`, `e2e/rescue-and-draft.spec.ts`, `e2e/helpers.ts` (добавить `interceptLead(page)`, `openCalculatorFromHero`, `completeAreaScreen(area)`).
**Сделать:** реализовать сценарии **1, 2, 3, 6, 7, 8, 9** из T‑091 v1 дословно, плюс новые:
- **11.** Шаг 1 на 1440×900: `boundingBox` контейнера `.calculator-modal-content` ≥ 480 px высоты (после N‑010).
- **12.** Шаг 0 на 390×844: первый экран показывает переключатель «Комната/Объект» **и** степпер площади без скролла (после N‑013).
- **13.** Страница услуги: сумма из hero === сумма из секции цены (после N‑002).
- **14.** Каталог на странице света: у первых 12 карточек `img[src^="/catalog/"]` и `naturalWidth > 0` (после N‑020).
- **15.** Экран «Светильники» Шага 1: «Добавить N популярных» → прогресс N/N и «Подтвердить» активна (после N‑021).
- **16.** Отправка лида → `GET /api/lead/:code` (внутренний, защищён `CRON_SECRET`) возвращает запись (после N‑001).
Все сценарии — в обоих проектах Playwright (desktop + mobile), кроме явно помеченных размером. Перехват `/api/lead` через `page.route` с проверкой `lead_kind`, `source`, `snapshot.rooms`, `lighting.items[].vendorCode`.
**Приёмка:** `npm run test:e2e` — 16 сценариев × 2 проекта зелёные в CI; в `ci.yml` job `e2e` обязателен для merge.

### N‑010 · Раскладка модалки: контент ≥ 55 % высоты, двухколоночный desktop · 📸 (F‑01, F‑02, F‑05, F‑12, F‑18)
**Файлы:** `components/calculator-modal/calculator-modal.tsx`, `lighting-footer-progress.tsx`, `price-strip.tsx`, `components/lighting/LightingCartDrawer.tsx`, `app/globals.css`.
**Сделать:**
1. **Desktop ≥ 1024 px:** панель `max-w-[1200px] max-h-[92dvh] rounded-3xl`, сетка `grid-cols-[1fr_360px]`. Левая колонка — шапка (одна строка: заголовок + ✕) + прокручиваемый контент + футер с кнопками. Правая колонка — липкая «Сводка»: PriceStrip (развёрнутый: строки по комнатам/свету, итог), под ним на Шаге 1 — «Прогресс подбора» и мини‑корзина (список 3 последних + «Все N»). Плавающая круглая иконка (F‑05) удаляется — её функцию берёт правая колонка.
2. **Mobile < 1024 px:** шапка сжимается до одной строки «Параметры потолка · 3/8 · ✕» (макро‑чипы убрать, см. N‑011); PriceStrip — одна строка «~18 000 ₽ ▾» высотой 44 px, по тапу раскрывается bottom‑sheet со сводкой; «Прогресс подбора» — одна строка над кнопками («Светильники 0/6 · Добавить 6») высотой ≤ 56 px; блок «У меня уже есть освещение / Пропустить» — ссылка «Пропустить свет» в футере рядом с «Назад» (текстовая, не кнопка).
3. Бюджет высоты (mobile 844 px): шапка 52 + стрип 44 + футер (прогресс 56 + кнопки 64 + safe‑area) ≤ 240 px → контенту ≥ 600 px. Desktop 900 px: шапка 64 + футер 80 → контенту ≥ 700 px.
4. `--z-*` токены без изменений; `aria-live="polite"` на сумме сохранить.
**Приёмка:** e2e сц. 11, 12; скриншоты до/после в PR; Lighthouse a11y модалки ≥ 95.

### N‑020 · Локальные фото каталога в сборке · (F‑14, F‑40; закрывает T‑061 остаток)
**Файлы:** `scripts/build-catalog-images.mjs`, `package.json` (`prebuild`), `.github/workflows/ci.yml`, `components/feed2/ProductImage.tsx`, `data/catalog-images.json`, `.gitattributes`/`.gitignore` (решение о хранении), `README.md`.
**Сделать:**
1. `build-catalog-images.mjs`: параллельность 6, таймаут 8 с, ретрай 2, User‑Agent браузерный, форматы WebP 256/512 (качество 78), запись `data/catalog-images.json` `{ productId: { w: 512, h: 512, hash } }`. Товары без обложки → в отчёт `data/catalog-images-missing.json`.
2. Стратегия хранения: **коммитить `public/catalog/*.webp` в репозиторий** (≈ 547 × 2 × ~25 КБ ≈ 27 МБ — приемлемо; при росте > 100 МБ перейти на Git LFS или объектное хранилище провайдера). Обновление — вместе с фидом (N‑041).
3. `ProductImage`: если `productId ∉ catalog-images` **и** хотлинк не загрузился — заглушка с **названием товара** и иконкой типа (профиль/светильник/лампа/БП), а не текст «Фото товара».
4. `prebuild` — только `check-catalog-images` (fail, если покрытие < 90 %), скачивание — вручную/в отдельном job `refresh-catalog` с ручным триггером `workflow_dispatch`.
**Приёмка:** `tests/catalog-images.test.ts` — покрытие ≥ 95 % товаров с `priceRub > 0`; e2e сц. 14; размер `public/catalog` в PR.

---

## ФАЗА B — P1 (недели 2–3). Конверсия квиза и страницы света

### N‑011 · Один индикатор прогресса · (F‑03; закрывает T‑030 п. 2)
**Файлы:** `calculator-modal.tsx` (`StepNav`), `lib/calculator/fsm.ts` (`calcProgress`), `lib/calculator/selectors.ts` (`selectProgress`).
**Сделать:** оставить **только** тонкую полоску с подписью «Потолок · вопрос 3 из 8» / «Свет · шаг 2 из 4» / «Итог»; макро‑шаги — в подписи, не чипами. Полоска 3‑сегментная (потолок / свет / итог), заполненный сегмент — пройденный макро‑шаг; клик по пройденному сегменту — переход назад (с `aria-label`). `M` фиксирован для сценария (не меняется при ответах).
**Приёмка:** grep `Шаг \d+ из` → один компонент; скриншот mobile: индикатор ≤ 24 px высотой; vitest `calcProgress(standard) === 5`, `(modern) === 8`, `(advanced) === 9` (проверить фактические M и зафиксировать).

### N‑012 · PriceStrip: сумма появляется после первого ответа, одна строка, объяснение минимума · (F‑04, F‑12)
**Файлы:** `price-strip.tsx`, `lib/calculator/selectors.ts` (`selectStripState`), `content/homepage.ts` (текст).
**Сделать:**
1. Состояния стрипа (селектор): `idle` (ни один параметр не подтверждён) → текст «Ответьте на 5 вопросов — покажу ориентир»; `estimating` (подтверждена площадь) → «~18 000 ₽ · минимальный заказ» с тултипом‑раскрытием «В минимальный заказ 18 000 ₽ входит выезд, замер и монтаж до 18 м²»; `detailed` → «~44 000 ₽ · потолок 38 000 + свет 6 000 (−25 %)».
2. Формат: один `·` с пробелами, `₽` неразрывным пробелом, число `toLocaleString("ru-RU")`; никакого дубля «Потолок X · Итого X» при равенстве сумм.
3. Анимация изменения суммы — только числа (`animKey` оставить), без мигания всего блока.
**Приёмка:** vitest `selectStripState` для 3 состояний; e2e сц. 1 проверяет текст стрипа на экране площади до и после подтверждения.

### N‑013 · Экран «Площадь»: порядок, без имени комнаты на входе, язык клиента · 📸 (F‑08–F‑11, F‑13)
**Файлы:** `quiz-v2/screens/ParamScreen.tsx` (ветка `area`), `quiz-v2/ui.tsx` (`RangeField`), `lib/calculator-v2/labels.ts`, `SummaryScreen.tsx`, `RoomEditScreen.tsx`.
**Сделать:**
1. Порядок блоков: **(a)** «Что считаем?» — «Одну комнату» / «Всю квартиру или дом» (карточки с иконкой, 2 в ряд на mobile); **(b)** «Площадь потолка» — степпер 44 px + 5 чипов (`12 · 15 · 18 · 22 · 30` для комнаты; `40 · 60 · 80 · 100 · 120` для объекта), слайдер — только `lg:`; **(c)** «Название» — свёрнутая ссылка «Назвать помещение (например, Кухня)» → раскрывает поле. При добавлении 2‑й комнаты поле раскрыто по умолчанию, дефолт «Комната 2».
2. Тексты: заголовок «Площадь потолка», подзаголовок «Только площадь. Профили, карнизы и свет спрошу дальше — по метрам». Плашка предзаполнения — только при `prefilled.area === true` **и** с указанием источника: «Подставил 22 м² со страницы «Теневой профиль» — поправьте под себя».
3. `RangeField` — управляемый компонент без `useEffect`; `aria-valuetext` «18 квадратных метров».
**Приёмка:** e2e сц. 12; axe 0 critical; grep «узл|участк» в экранах квиза → 0.

### N‑021 · Шаг 1 «Светильники»: выбор по картинке, «Добавить N популярных», цоколь как вторичный фильтр · 📸 (F‑15, F‑16, F‑45)
**Файлы:** `wizard-step1-lighting.tsx` (после N‑051 — `components/lighting/PointsStep.tsx`), `lib/lighting/kit-rules.ts` (`popularPoints(required, socketPreference)`), `lib/catalog-ui-config.ts`, `content/lighting-copy.ts` (новый).
**Сделать:**
1. Экран «Светильники» открывается с **3 карточками‑типами** с фото и ценой «от»: «Классические врезные (GX53) — от 350 ₽», «Тонкие поворотные (MR16/GU10) — от 590 ₽», «Панели — от 890 ₽» (цены считать из фида: минимальная `priceRub` доступного товара типа). Под ними — кнопка «Добавить 6 популярных {тип} за {sum} ₽» → `applySuggestion` (позиции `auto: "point"`), прогресс сразу 6/6, «Подтвердить светильники» активна.
2. Табы цоколей и поиск — под карточками, в свёрнутом «Выбрать самому» (раскрыто, если пользователь уже что‑то добавил вручную).
3. Копирайт: «точки» → «светильники» везде в UI (футер: «Светильники 6/6»); подпись «Лампы к ним предложу ниже» (T‑012).
4. Секция «Комплектующие» (T‑012/T‑042) — сразу после подтверждения светильников, не над «Выбранным».
**Приёмка:** e2e сц. 15; vitest `popularPoints(6, "GX53")` возвращает 6 шт. одного SKU с `available` и минимальной ценой; grep «точк» в `components/calculator-modal/**` и `components/lighting/**` → только `pricing.spotInstall` подписи «монтаж светильника».

### N‑030 · Главная: hero с продуктом, без дублей, один primary · 📸 (F‑22–F‑24, F‑26–F‑28)
**Файлы:** `content/homepage.ts` (`hero`, `trust.founder`), `content/home-assets.ts`, `components/home/home-hero.tsx`, `founder-block.tsx`, `home-proof.tsx`, `home-promise.tsx`, `mobile-sticky-cta.tsx`, `public/optimized/*`.
**Сделать:**
1. Hero: фон — фото объекта (`proj-float-1` или `proj-shadow-1` в AVIF 1440 ≤ 150 КБ), поверх — H1 «Теневые и парящие потолки. Делаю сам, цена фиксируется до монтажа», подзаголовок «От 1 000 ₽/м² · ориентир за 2 минуты», primary «Рассчитать за 2 минуты», secondary — **текстовая ссылка** «Записаться на замер» (на mobile тоже ссылка, не вторая кнопка), строка фактов **один раз**: «5.0 на Avito · 19 отзывов · договор и гарантия 2 года». Портрет — круглый 72 px с подписью «Владимир, мастер» в углу карточки hero.
2. Блок «Работаю лично»: портрет `about-master.jpeg` остаётся здесь **единственным** местом полноразмерного портрета.
3. Схема фонов: hero тёмный → работы **светлый** → цена светлый (с тонированной картой) → о мастере светлый → процесс **тёмный** → отзывы светлый → заявка светлый. Максимум 2 тёмные секции.
4. `MobileStickyCta` появляется после прокрутки hero; одна кнопка «Рассчитать» + телефон‑иконка.
**Приёмка:** скриншоты; e2e: в viewport hero ровно один `button[data-cta="primary"]`; Lighthouse mobile LCP < 2,5 с на throttled 4G (отчёт в PR).

### N‑031 · Округление ориентиров, ценовые примеры вместо схемы шагов · (F‑25, F‑29)
**Файлы:** `lib/home-proof-pricing.ts` (`toProofPriceLabel`), `content/homepage.ts` (`price.examples`), `components/home/home-price.tsx`.
**Сделать:** `toProofPriceLabel` округляет до 1 000 ₽ при сумме > 50 000 и до 500 ₽ ниже: «≈ 107 000 ₽», «≈ 276 000 ₽»; секция «Быстрый ориентир по цене» — три примера, вычисляемые из `pricing` (не литералы): «Спальня 12 м², простой — 18 000 ₽ (минимальный заказ)», «Кухня‑гостиная 24 м², теневой по периметру 20 м.п. — 43 000 ₽», «Гостиная 30 м², парящий 22 м.п. + трек 6 м — 94 000 ₽»; каждая карточка — кнопка «Посчитать похожее» → `openCalculator` с пресетом примера.
**Приёмка:** vitest на округление и на суммы примеров (пересчёт через `calcRoom`); e2e: клик по примеру открывает квиз с площадью 24.

### N‑032 · Страницы услуг: CTA в середине, «Хочу так же», аргументированный кросс‑селл, hero‑визуал · 📸 (F‑33, F‑35–F‑37)
**Файлы:** `app/uslugi/_components/ServicePageLayoutV2.tsx`, `ServiceProofStrip.tsx`, `ServiceRelatedServices.tsx`, `ServiceHero.tsx`, `content/services.ts` (`proof.items[].actionPreset`, `related[].reason`), `content/service-links.ts`.
**Сделать:**
1. После `ServiceCompareSection` (или `UseCases`, где сравнения нет) — компактная CTA‑полоса «Ориентир для вашей комнаты за 2 минуты → [Рассчитать]» (тот же пресет).
2. Карточки примеров: кнопка «Хочу так же» → `openCalculator({ preset: item.actionPreset, source: "<slug>:proof" })` (как на главной).
3. Смежные услуги: поле `reason` («Теневой + встроенный трек: одна закладная, свет −25 %») — рендерить под ценой.
4. Hero‑изображение: `aspect-[4/3]`, ≥ 40 % ширины на desktop, для каждой услуги выбрать фото с характерным эффектом (список в коммите). На mobile — под H1, до CTA не показывать (CTA в первом экране).
5. Мобильная липкая панель «от 950 ₽/м.п. · [Рассчитать]» появляется после hero (переиспользовать `MobileStickyCta` с `priceAnchor`).
**Приёмка:** e2e: на `/uslugi/tenevoy-profil` ≥ 2 входа в калькулятор до формы; скриншоты mobile первого экрана всех 9 услуг — CTA видна без скролла.

### N‑040 · Страница света: комплекты как офферы, объяснение систем до каталога, чистый hero · 📸 (F‑41–F‑43, F‑46)
**Файлы:** `LightKitShowcase.tsx`, `page.tsx` (порядок секций), `TrackSaleSystemGuideSection.tsx`, `ServiceHero.tsx` (ветка света), `content/services.ts` (тексты страницы света), `components/home/avito-reviews-section.tsx` (проп `filter`).
**Сделать:**
1. Карточка комплекта: фото сцены (сгенерировать/подобрать 3 изображения 4:3 ≤ 80 КБ), заголовок «Кухня до 12 м²», строка состава человеческим языком «3 м профиля · 4 светильника · питание · монтажный набор», **одна** цена (с потолком, крупно) + зачёркнутая базовая + строка «только оборудование: X ₽», кнопка «Взять этот комплект», ссылка «Состав по артикулам ▾» (раскрывает список SKU).
2. Порядок секций: hero → «Какая система вам подойдёт» (3 карточки COLIBRI/CLARUS/ART с фото узла, 3 отличиями и кнопкой «Показать товары») → «Готовые комплекты» → «Каталог» (с предвыбранной системой из карточки) → «Как купить» → «Условия» → FAQ → отзывы (с фильтром по тегу `light`, если < 2 — подпись «отзывы по монтажу») → форма → смежные.
3. Hero: убрать чипы‑штампы скидок; оставить строку «−10 % на комплект · −25 % при заказе потолка · наличие по прайсу от {updatedAt}»; «от 2 418 ₽» → округление до 100 («от 2 400 ₽») через общий `roundAnchor`.
**Приёмка:** на карточке комплекта ≤ 2 числа до раскрытия; e2e сц. 5, 6 зелёные; скриншоты.

### N‑041 · Процесс обновления фида и видимая актуальность · (F‑44)
**Файлы:** `scripts/refresh-feed.mjs` (новый), `.github/workflows/refresh-catalog.yml` (новый, `workflow_dispatch` + `schedule: 0 6 * * 1`), `lib/lighting/catalog-index.ts` (`isStale(updatedAt, 45 дней)`), `CatalogSectionClient.tsx`, `README.md`.
**Сделать:** скрипт скачивает фид EKS (URL из `FEED_URL` секрета), нормализует в `eks-feed2-snapshot.json`, запускает `validate-catalog`, `build-catalog-index`, `build-catalog-images`, открывает PR «chore: обновление каталога {дата}» с диффом цен (топ‑20 изменений в теле PR). На странице: если `isStale` — плашка «Цены могли измениться — уточню перед счётом» вместо даты.
**Приёмка:** ручной запуск workflow создаёт PR; vitest `isStale`.

---

## ФАЗА C — P2 (недели 4–6). Архитектура, контент, токены

### N‑050 · Завершить единый стор: `store.tsx`, `useCalculator`, удалить сеттер‑колбэки и `setState` в эффектах · (F‑06; закрывает T‑030)
**Файлы:** новые `lib/calculator/store.tsx`, `lib/calculator/pricing.ts`, `lib/calculator/adapters.ts`; переписать `PriceCalculatorQuizV2.tsx`, `wizard-step0-calculator.tsx`, `wizard-step1-lighting.tsx` (после N‑051 — его части), `wizard-step2-summary.tsx`, `price-strip.tsx`, `lighting-footer-progress.tsx`, `mobile-sticky-cta.tsx`, `action-form.tsx`; **удалить** `lib/calculator-v2/use-ceiling-calculator-engine.ts`, `components/home/price-calculator-context.tsx`, сократить `calculator-modal-context.tsx` до UI‑состояния (`isOpen`, `sessionId`, `showResult`).
**Сделать:**
1. `CalculatorProvider` в `app/providers.tsx`: `useReducer(calculatorReducer)` + persist черновика (`draft.ts`) через `useSyncExternalStore`/подписку, не через `useEffect(setState)`.
2. `useCalculator()` → `{ state, dispatch, ...selectors }`; все прежние `engine.*` методы — `dispatch({ type })`.
3. Удалить `onStep0FooterActionChange`, `onStep0BackActionChange`, `setStep1FooterAction`, `onProgressChange` (34 вхождения) — модалка читает `selectFooterAction/selectBackAction/selectProgress`.
4. `adapters.ts`: `toLeadSnapshotV2(state)` — единственная точка сборки payload для формы и письма.
**Приёмка:** grep `useEffect\(` с `set[A-Z]\w+\(` в `components/calculator-modal/**` и `components/lighting/**` → 0 (исключения: `setMounted`, фокус, `requestAnimationFrame` анимации — перечислить в комментарии); grep `use-ceiling-calculator-engine|price-calculator-context` → 0; все e2e зелёные; `tests/reducer.test.ts`, `selectors.test.ts` расширены на footer/back/progress.

### N‑051 · Разбить Шаг 1 и каталог страницы на общие компоненты `components/lighting/*` · (F‑07; закрывает T‑031 п. 1)
**Файлы:** новые `components/lighting/LightingCatalog.tsx` (сетка + секции + поиск, ≤ 400 строк), `ProductCard.tsx`, `SystemPicker.tsx`, `PointsStep.tsx`, `TrackProfileStep.tsx`, `TrackFixturesStep.tsx`, `AccessoriesStep.tsx`, `SelectedList.tsx`, `ProductZoom.tsx`; `wizard-step1-lighting.tsx` → оркестратор ≤ 250 строк; `CatalogSectionClient.tsx` → обёртка ≤ 200 строк.
**Сделать:** вынести по границам текущих `if (wStep === …)`‑блоков; общие пропсы `{ mode: "modal" | "page", cart: useLightingCart() }`; поиск, зум, карточка — одна реализация. Скрипт `scripts/check-file-size.mjs` (в CI): любой `*.tsx` в `components/` и `app/` ≤ 600 строк.
**Приёмка:** `check-file-size` зелёный; e2e сц. 2, 5, 6, 15 зелёные; визуальный дифф карточки товара модалка ↔ страница — идентичен (скриншот‑тест Playwright `toHaveScreenshot` с порогом 0.1 %).

### N‑060 · Копирайт: язык клиента, первое лицо, конкретика · (F‑09, F‑17, F‑34, F‑38, F‑53)
**Файлы:** `content/services.ts`, `content/homepage.ts`, `content/lighting-copy.ts`, `lib/calculator-v2/labels.ts`, `lib/calculator-flow.ts`, `app/uslugi/_components/ServiceHero.tsx`.
**Сделать:**
1. Словарь замен (применить grep’ом по `content/` и `components/`): «узел» → «профиль»/«решение»; «точки» → «светильники»; «закладные» → «крепления»; «БП» → «блок питания»; «монтаж за 1 день» — оставить только там, где это верно для площади ≤ 30 м²; «мы/показываем/подберём» → «я/покажу/подберу».
2. Primary страниц услуг: «Рассчитать {название в вин. падеже}» — поле `hero.ctaObject` («теневой потолок», «парящий потолок», «световые линии», «трек в потолке», «скрытый карниз», «простой потолок»).
3. `about` каждой услуги — переписать по схеме «что видит клиент → цифра/факт → чего избегает»: минимум 2 конкретных числа на услугу (зазор 6–8 мм, экономия на плинтусе, срок, гарантия). Тексты — в описании коммита «было → стало», владелец подтверждает.
4. FAQ: добавить в каждую услугу вопрос «Что будет, если стены неровные / что если цена изменится после замера» с честным ответом (фиксация сметы).
**Приёмка:** grep словаря → 0 вне комментариев; `tests/copy-lint.test.ts` — запрет слов из словаря в `content/*.ts`; ревью владельца.

### N‑061 · Микро‑CTA после успеха, срочность, актуальный календарь · (F‑20, F‑21, F‑52)
**Файлы:** `calculator-modal.tsx` (экран успеха), `action-form.tsx`, `content/availability.ts`, `scripts/check-availability.mjs` (в CI: `validUntil` не дальше 21 дня), `components/home/home-trust.tsx`.
**Сделать:** на экране успеха — блок «Ускорить: пришлите 2–3 фото помещения в Telegram — уточню смету до выезда» с deep‑link `t.me/...?text=Заявка №{code}`; в hero главной и блоке «О мастере» — строка из `availability`: «Ближайшее окно на замер — {день}»; `validUntil` обязателен ≤ 21 дня вперёд (CI падает, если дальше или в прошлом — владелец обновляет еженедельно; при `validUntil` в прошлом блоки скрываются, а не врут).
**Приёмка:** e2e сц. 1 проверяет наличие Telegram‑ссылки с кодом на экране успеха; `check-availability` в `ci.yml`.

### N‑062 · SEO‑гигиена · (F‑30)
**Файлы:** `app/layout.tsx`, `content/homepage.ts` (`seo`), `app/sitemap.ts`, `public/optimized/`.
**Сделать:** `og:image` — `hero` 1200×630 WebP ≤ 200 КБ; `title` главной с интентом «цена» («Натяжные потолки: теневые и парящие — цена за 2 минуты | ПОТОЛКОВО»); `sitemap.lastModified` — из даты последнего коммита, затрагивающего страницу (`git log -1 --format=%cI -- <file>` в prebuild → `data/page-dates.json`).
**Приёмка:** Lighthouse SEO 100 на главной и 3 услугах; валидатор OG показывает картинку.

### N‑063 · Дизайн‑токены: один primary, радиусы, контраст · 📸 (F‑50, F‑51)
**Файлы:** `app/globals.css` (`@theme`), `components/ui/button.tsx`, `tests/design-tokens.test.ts`, все `className` с `rounded-*`/`bg-blue-600`/`bg-slate-950` в кнопках.
**Сделать:** primary — **один** цвет на сайте (решение: тёмный `slate-950` для действий с деньгами и синий `#2563eb` — убрать; или наоборот — зафиксировать в токене `--color-primary`); радиусы: карточки `--radius-card: 1.25rem`, контролы `--radius-control: 0.875rem`, чипы `9999px`; размеры кнопок только `sm 40 / md 44 / lg 52`; подписи — минимум `text-slate-600` на белом, 12 px минимум. `tests/design-tokens.test.ts` расширить: grep запрещённых классов (`rounded-xl` в кнопках, `bg-blue-600`, `text-slate-400` на тексте < 14 px).
**Приёмка:** axe contrast 0 нарушений на модалке и главной; тест токенов зелёный; скриншоты.

---

## 4. Матрица «находка аудита → задача»

| Находки | Задача |
|---|---|
| F‑01, F‑02, F‑05, F‑12, F‑18 | N‑010 |
| F‑03 | N‑011 |
| F‑04, F‑12 | N‑012 |
| F‑06 | N‑050 |
| F‑07 | N‑051 |
| F‑08–F‑11, F‑13 | N‑013 |
| F‑14, F‑40 | N‑020 |
| F‑15, F‑16, F‑45 | N‑021 |
| F‑17, F‑09, F‑34, F‑38, F‑53 | N‑060 |
| F‑19, F‑48 | N‑001 |
| F‑20, F‑21, F‑52 | N‑061 |
| F‑22–F‑24, F‑26–F‑28 | N‑030 |
| F‑25, F‑29 | N‑031 |
| F‑30 | N‑062 |
| F‑31, F‑32, F‑39 | N‑002 |
| F‑33, F‑35–F‑37 | N‑032 |
| F‑41–F‑43, F‑46 | N‑040 |
| F‑44 | N‑041 |
| F‑47 | владелец (реквизиты) |
| F‑49 | N‑003 |
| F‑50, F‑51 | N‑063 |

## 5. Незакрытые задачи v1, включённые в v2

| v1 | Что осталось | v2 |
|---|---|---|
| T‑020 | литералы 25/10 в 3 файлах; якоря услуг | N‑002 |
| T‑027 | БД, серверный дедуп/лимит, ретраи | N‑001 |
| T‑030 | `store.tsx`, `useCalculator`, сеттер‑колбэки, `setState` в эффектах, один индикатор | N‑050, N‑011 |
| T‑031 | общий `LightingCatalog.tsx` | N‑051 |
| T‑046 | H1 без региона, якорь из `pricing` | N‑002 |
| T‑061 | локальные фото каталога | N‑020 |
| T‑091 | e2e сц. 1, 2, 3, 6, 7, 8, 9 | N‑003 |

## 6. Действия владельца (параллельно фазе A)

1. Завести PostgreSQL (Neon free / Vercel Postgres / Amvera) → `DATABASE_URL` в окружение; после N‑001 выполнить `npx drizzle-kit push`.
2. Ротировать `AMVERA_API_KEY`; проверить `TELEGRAM_BOT_TOKEN/CHAT_ID`, `WEB3FORMS_ACCESS_KEY`, `CRON_SECRET`; настроить cron `/api/lead/retry`.
3. Заполнить `legalName / inn / ogrnip`.
4. Создать цели в Метрике по таблице README; добавить цели `lead_saved` (после N‑001) и `points_quick_add` (после N‑021).
5. Дать 6–9 фото объектов для hero услуг (N‑032) и 3 фото сцен для комплектов (N‑040) — или согласовать генерацию.
6. Еженедельно обновлять `availability.ts` (N‑061) — 2 минуты.

## 7. Definition of Done для всего ТЗ v2

- Все `N‑xxx` закрыты, три отчёта по фазам в репозитории.
- CI зелёный: lint, tsc, vitest, 16 e2e × 2 проекта, build, bundle ≤ 300 КБ, file‑size ≤ 600, gitleaks, check‑availability.
- Ручной прогон владельцем: заявка с главной → пришла в Telegram и на почту, найдена в БД по коду; заявка с `/uslugi/skrytye-karnizy` — `source` верный; комплект «Кухня» со страницы света → Шаг 2 «Только оборудование» → счёт получен.
- Lighthouse mobile главной: Performance ≥ 85, A11y ≥ 95, SEO 100.
