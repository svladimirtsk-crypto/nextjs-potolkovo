# Отчёт по фазе A (P0) — ТЗ v2

**Ветка:** `quizv2ver1` · **Диапазон:** `876d64c..a472bb1` · **Дата:** 2026-09-05

Фаза A закрыта: перестали терять заявки (лиды в БД), убрали расхождения цен,
заменили хотлинки на локальные фото, восстановили обязательный e2e-набор и
починили раскладку калькулятора на desktop.

---

## 1. Задачи

| Задача | Коммит | Закрывает | Статус |
|---|---|---|---|
| N‑001 · Лиды в PostgreSQL через Drizzle | `67a7356` | T‑027, F‑19, F‑48 | ✅ |
| N‑002 · Ценовые якоря из `pricing.ts`, H1 без региона | `ae8b4de` | T‑020, T‑046, F‑31, F‑32, F‑39 | ✅ |
| N‑020 · Локальные фото каталога | `bef851d` | T‑061, F‑14, F‑40 | ✅ с оговоркой |
| N‑003 · Обязательный e2e-набор | `1274f55` | T‑091, F‑49 | ✅ с оговоркой |
| N‑010 · Двухколоночная раскладка модалки 📸 | `a472bb1` | F‑01, F‑02, F‑05, F‑12, F‑18 | ✅ |

---

## 2. Зелёное состояние

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit` | чисто |
| `npm run lint` | 0 errors, 23 warnings |
| `npm run test` | **256 passed** (31 файл) |
| `npm run test:e2e` | **45 passed**, 0 failed, 4 skipped |
| `npm run build` | ok, 15 статических страниц |
| `npm run check:bundle` | `/` 212.3 КБ ≤ 300 КБ |

Из 4 skipped: 2 — mobile-only сценарии, пропускаемые в desktop-проекте
(это штатное поведение), 2 — `test.fixme`, см. раздел 4.

---

## 3. Целевые метрики v2

| Метрика | Цель | Факт | |
|---|---|---|---|
| Лид сохранён в БД и доступен по `leadId` | 100 % | 100 % | ✅ |
| Карточки каталога с локальным фото | ≥ 95 % | **94 %** (513/547) | ⚠️ |
| Видимая область контента Шага 1 (desktop) | ≥ 480 px | **642 px** | ✅ |
| Видимая область Шага 0 (mobile) | ≥ 480 px | **692 px** | ✅ |
| Обязательные e2e T‑091 | 10 из 10 | 8 из 10 полностью, 1 частично | ⚠️ |
| Расхождение цены hero ↔ секция ↔ `pricing.ts` | 0 | 0 | ✅ |
| Источников состояния калькулятора | 1 | 3 | ⏭ N‑050 |
| Строк в самом большом клиентском файле | ≤ 600 | 2 248 | ⏭ N‑050 |

Последние две — задачи фазы B, в рамках A не трогались.

---

## 4. Что не закрыто и почему

### 4.1. Покрытие фото 94 %, а не 95 %

34 товара из 547 не имеют обложки: у поставщика по этим URL **реальный 404**
(проверено `curl`, отдаётся HTML-страница ошибки). Это не дефект скрипта —
список в `data/catalog-images-missing.json`. Потолок достижимого сейчас — 94 %.

Порог стража `scripts/check-catalog-images.mjs` выставлен на 90 %, чтобы сборка
не падала из-за чужих 404, но и не деградировала незаметно.

**Действие владельца:** запросить у поставщика обложки для 34 позиций.

### 4.2. Дефолтная система каталога — COLIBRI_220, полностью без фото

Все 16 трековых светильников COLIBRI входят в те самые 34. Каталог на странице
света открывается на этой системе, поэтому первый экран — сплошные заглушки,
хотя механизм работает: переключение на CLARUS_48 даёт **24/24 локальных фото,
0 битых**. Товары с фото подняты в сортировке выше, но внутри COLIBRI поднимать
нечего.

**Решение владельца:** сменить систему по умолчанию на CLARUS_48 (одна строка,
сильно меняет первое впечатление) — не делал без согласования, это изменение
поведения UI.

### 4.3. T‑091 сц. 3 закрыт частично

Сценарий требует, чтобы CTA страницы услуги открывала квиз с предзаполненным
типом потолка и площадью 22 м². Фактически калькулятор открывается пустым
(сценарий «Комнату», дефолт 10 м²) — тип услуги не переносится.

Это следствие трёх источников состояния Шага 0. Тест написан и помечен
`test.fixme` в `e2e/service-preset.spec.ts` — включится тем же коммитом, что и
починка в **N‑011/N‑050**. Подгонять его под текущее поведение не стал.

### 4.4. Черновик расчёта не сохраняется

T‑091 сц. 9 упоминает «черновик предлагается». В коде такой функциональности
нет: после закрытия модалки `localStorage` пуст, повторное открытие начинает
с нуля. Сценарий 9 написан на реальный контракт: после успешной отправки
rescue-диалог не показывается и дубль лида не уходит — это работает верно.

### 4.5. Сц. 2 и 6 не автоматизированы

Сценарии с автосборкой комплекта COLIBRI и переходом «комплект → квиз» требуют
стабильных селекторов внутри Шага 1 (2 248 строк). Разумнее писать их после
распила файла в N‑050/N‑051, иначе тесты придётся переписывать дважды.

---

## 5. Замечания по расхождениям с аудитом

**Метрика «видимая область Шага 1 ≈ 150 px» не подтвердилась.** До правок
фактический замер `boundingBox` давал **643 px** на desktop. Реальная проблема
была не в высоте контента, а в ширине панели: модалка растягивалась на весь
экран (1440 px), контент жался слева, сумма уезжала из поля зрения. Это и
починено в N‑010.

---

## 6. Ключевые инварианты, закреплённые тестами

- **N‑001:** окно дедупа и rate-limit считаются часами БД (`now() - make_interval`),
  сравнение строгое `>`; лимит держится на БД, а не на памяти процесса.
- **N‑002:** ни одного ценового литерала в `content/services.ts` (включая FAQ)
  и компонентах — только `servicePriceAnchor()` / `pricing.*`. H1 без региона,
  регион в `title`/`description`. Сторож — `tests/service-price-anchor.test.ts`.
- **N‑020:** сборка не ходит в сеть; фото лежат в репозитории (12 МБ, порог
  перехода на Git LFS ~100 МБ зафиксирован в `.gitignore`); `prebuild` падает
  при покрытии < 90 %.
- **N‑010:** на desktop панель уже вьюпорта и имеет колонку сводки; на mobile
  остаётся полноэкранной. Проверяется `boundingBox`, а не наличием классов.

---

## 7. Действия владельца

1. **PostgreSQL:** задать `DATABASE_URL`, выполнить `npx drizzle-kit push`.
   Без этого лиды пишутся в память и теряются при перезапуске.
2. **Ротировать `AMVERA_API_KEY`** (светился ранее).
3. **Заполнить реквизиты** `legalName` / `inn` / `ogrnip` (сейчас `TODO_OWNER`),
   иначе `npm run check:legal` не пропустит релиз.
4. **34 товара без фото** — запросить обложки у поставщика.
5. **Решить по COLIBRI** — см. п. 4.2.
6. **Цели в Метрике:** добавить `lead_saved`, `points_quick_add`.
7. **Контент:** 6–9 фото объектов и 3 фото сцен.
8. **Еженедельно** обновлять `content/availability.ts`.
9. **Cron** на `/api/lead/retry` (`*/15 * * * *`) с `CRON_SECRET` — иначе
   неудачные доставки в Telegram не переотправятся.

---

## 8. Скриншоты 📸 (N‑010)

| Файл | Что показывает |
|---|---|
| `shots/n010-before-step0-desktop.png` | Шаг 0 до: панель во весь экран 1440 px |
| `shots/n010-before-step1-desktop.png` | Шаг 1 до: контент слева, справа пустота |
| `shots/n010-after-step0-desktop.png` | Шаг 0 после: окно 1200 px + сводка |
| `shots/n010-after-step1-desktop.png` | Шаг 1 после: двухколоночная раскладка |
| `shots/n010-before-*-mobile.png`, `shots/n010-after-*-mobile.png` | Mobile 390×844: без регрессий |
| `shots/n020-after-{desktop,mobile}.png` | Каталог с локальными фото |

---

## 9. Следующий шаг — фаза B

Первыми: **N‑011** (один индикатор прогресса) и **N‑050** (единый источник
состояния, распил `wizard-step1-lighting.tsx` 2 248 → ≤ 600 строк). Они
разблокируют `test.fixme` из п. 4.3 и сценарии из п. 4.5.
