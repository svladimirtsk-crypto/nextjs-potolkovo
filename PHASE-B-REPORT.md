# Фаза B (P1) — отчёт

Ветка `quizv2ver1`, коммиты `25e5c00…56185b7`. Все 9 задач закрыты, зелёное
состояние подтверждено.

## Зелёное состояние

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit` | чисто |
| `npm run lint` | 0 ошибок, 20 warnings — базовый уровень не вырос |
| `npm run test` | **436 passed**, 4 skipped (было 370) |
| `npm run test:e2e` | **87 passed**, 11 skipped (было 49) |
| `npm run build` | 18/18 страниц |
| `npm run check:bundle` | главная **214.7 КБ** ≤ 300 КБ |
| `check-file-size` | ok, 4 legacy-исключения |

## Что сделано

| Задача | Находки | Коммит |
|---|---|---|
| N‑011 | индикатор шага | (фаза A) |
| N‑012 | F‑04, F‑12 | `25e5c00` |
| N‑031 | F‑25, F‑29 | `3539f3e` |
| N‑013 📸 | F‑08–F‑11, F‑13 | `6ebbed2` |
| N‑021 📸 | F‑15, F‑16, F‑45 | `1f46aa6` |
| N‑032 📸 | F‑33, F‑35–F‑37 | `0d76c9f` |
| N‑040 📸 | F‑41–F‑43, F‑46 | `67b8cef` |
| N‑041 | F‑44 | `56185b7` |

Скриншоты до/после (1440×900 и 390×844) — `shots/n013/`, `shots/n021/`,
`shots/n032/`, `shots/n040/`.

### Сквозная тема фазы: цифра должна отвечать на заданный вопрос

Четыре независимые задачи оказались про одно и то же — про цену, которая
выглядит точнее, чем есть.

**N‑012.** Полоса показывала 18 000 ₽ сразу при открытии модалки. Это была
сумма дефолтной комнаты из стора — ответ на незаданный вопрос. Признаком
реального ответа стал `step0SessionInteracted`, а не наличие комнаты.

**N‑031.** «≈ 107 426 ₽» обещает смету, а получено делением площади на ставку.
`roundAnchor` округляет до 1 000 ₽ выше 50 000 и до 500 ₽ ниже. Отдельно
`roundFromAnchor` округляет «от»-цены **вниз** — иначе округлённое «от»
оказывается выше реальной минимальной цены.

**N‑040.** «от 2 418 ₽» в hero — та же болезнь, закрыта тем же `roundFromAnchor`.

**Общее правило, выведенное по ходу:** бизнес-флаг нельзя выводить сравнением
сумм. `ceilingTotal === pricing.minimumOrderRub` даёт ложное срабатывание на
18 м² × 1000 ₽ — совпадение случайно. Берём `snapshot.minimumOrderApplied`.

### Вторая тема: язык монтажника в интерфейсе клиента

- N‑013: «профили и узлы по нужным участкам в метрах» → «Только площадь.
  Профили, карнизы и свет спрошу дальше — по метрам». Grep «узл|участк» по
  экранам Шага 0 даёт 0.
- N‑021: «точки» → «светильники» по всему UI, включая письмо менеджеру.
- N‑032: «Рассчитать с этим узлом» → «Рассчитать этот потолок».

## Расхождения с ТЗ v2

Документ писался до сверки с кодом, поэтому часть цифр в нём не совпала с
реальностью. Везде выбран факт, а не документ.

| Место | В ТЗ | Фактически |
|---|---|---|
| N‑012, текст полосы | «Ответьте на 5 вопросов» | 6 / 8 / 8 по сценарию (`maxParamsForScenario`) |
| N‑031, пример кухни | 43 000 ₽ | **38 000 ₽** по действующему прайсу |
| N‑021, цены типов | 350 / 590 / 890 ₽ | **68 / 180 / 260 ₽** из фида |

Во всех трёх случаях значения вычисляются, а не записаны литералами: разойтись
с калькулятором и прайсом они уже не могут.

## Ограничение п. 0.4 и как оно обойдено

N‑032 по ТЗ требовала дописать `actionPreset` и `reason` в `content/services.ts`,
но правка контента разрешена только в N‑002/030/040/060. Ничего не дописано:
площадь примера выводится из существующего `areaLabel`, довод связки — из пары
слагов в `lib/service-page-actions.ts`. Для N‑040 правка была разрешена, но не
понадобилась.

## Баги, найденные тестами (не глазами)

1. **`\b` после кириллицы.** `/(\d+)\s*м\b/` не матчит «2 м чёрный»: граница
   слова считается по латинице. N‑040.
2. **Длина профиля в фиде — габариты.** У поставщика «2000*62*51 мм», а не
   «2 м». После починки п. 1 тесты были зелёными, а живая карточка показывала
   состав без метров. N‑040.
3. **Дельта цен врала.** Ключ вычёркивался из map только при изменении цены —
   товары с неизменной ценой попадали в «пропало из фида»: 543 пропажи на 547
   позиций. N‑041.
4. **Непокрытые пары кросс-селла.** Тест «каждая связка каждой страницы»
   поймал `skrytye-karnizy → trekovoe` и `prostye → tenevoy`. N‑032.
5. **`??` пропускает явный `null`.** В `initFromPreset` заглушающий `null`
   проваливался в дефолтный текст. Заменено на `!== undefined`. N‑013.

## Дефекты, найденные на скриншотах

Оба не видны в тестах и не описаны в аудите — только глазами на 390×844.

- **N‑021:** главная кнопка «Добавить 6 популярных» стояла после ссылки
  ручного выбора и уходила под сгиб. Переставлена сразу за карточками типов.
- **N‑013:** дефолт площади `DEFAULT_CALCULATOR_AREA = 10` не совпадал ни с
  одним быстрым чипом — ни один не подсвечен, экран выглядел как «выбор не
  сделан». Привязано к `pricing.defaults.roomArea` = 18.

## Побочные находки

- **`ServiceHeroCta` открывала калькулятор без пресета** — главная кнопка
  страницы услуги теряла то, ради чего человек на неё пришёл. Исправлено в
  N‑013.
- **Плашка предзаполнения на главной.** Корень глубже разметки: `openCalculator`
  подставляет заглушку `{standard, 10 м²}`, поэтому `prefilled.area` выставлялся
  всегда. Введён `presetOrigin: "page" | "default"`.
- **Цена и кнопка вели в разные места.** На карточке комплекта крупная цена —
  с потолком, а первичная кнопка оформляла только оборудование. N‑040.

## Размеры файлов

| Файл | Было | Стало |
|---|---|---|
| `price-strip.tsx` | 190 | **139** |
| `wizard-step1-lighting.tsx` | 1685 | **1677** |
| `CatalogSectionClient.tsx` | 878 | **869** |

Страж срабатывал дважды — на N‑021 (1730 строк) и N‑041 (896). Оба раза код
выносился в модули: `PointsScreen`, `pointProgressBySocket`, `CatalogFreshness`.

## Не сделано и требует владельца

1. **Hero-фотографии услуг (F‑35, часть N‑032).** Сделана только раскладка:
   равные колонки, на mobile фото после CTA. Подбор снимков с характерным
   эффектом для каждой из 9 услуг — работа с реальными фото объектов, которых
   в репозитории нет. Для теневого профиля до сих пор показан угол с бежевой
   стеной вместо теневой линии.

2. **`.github/workflows/refresh-catalog.yml` не запушен.** Файл готов и лежит в
   рабочей копии. PAT без scope `workflow` — GitHub отклоняет push целиком.
   Нужно добавить файл вручную или выдать токену scope.

3. **Секрет `FEED_URL`** в настройках репозитория — без него workflow упадёт.

4. **Отзывы про свет (F‑46).** По теме нашёлся 1 отзыв из 16 — порог в 2 не
   пройден, показываются общие с подписью «Отзывы по монтажу потолков». Нужны
   реальные отзывы про подбор света и доставку.

5. **Lighthouse mobile LCP** (метрика N‑030) в песочнице недостоверен — нужен
   прогон на боевом хостинге.

## Метрики v2

| Цель | Статус |
|---|---|
| Лид в БД 100 % | ✅ |
| Фото ≥ 95 % | 94 % (34 товара без фото — у поставщика) |
| Шаг 1 desktop ≥ 480 px | ✅ 642 |
| Шаг 0 mobile без скролла | ✅ |
| e2e сценарии | 15 ✅ (N‑021), 16 ✅ (N‑001); осталось 2 из 16 |
| Источников состояния | 2 из 1 — закрывается в N‑050 (фаза C) |
| Макс. файл ≤ 600 строк | 1677 — фаза C |

## Готовность к фазе C

Фаза B зелёная, п. 0.2 соблюдён. N‑050 и N‑051 частично сделаны авансом
(остановлены по решению владельца: `CatalogSectionClient.tsx` 869 при цели
≤200, `wizard-step1-lighting.tsx` 1677 при цели ≤250).
